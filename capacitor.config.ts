import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tfg.web',
  appName: 'tfg-web',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
